query getMenu {
  menus @context(provider: "vtex.mega-menu") {
    id
    name
    slug
    menu {
      id
      name
      slug
      menu {
        id
        name
        slug
      }
    }
  }
}
