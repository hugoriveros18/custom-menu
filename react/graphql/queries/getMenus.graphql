query getMenu {
  menus @context(provider: "vtex.mega-menu") {
    id
    name
    slug
    order
    menu {
      id
      name
      slug
      order
      menu {
        id
        name
        slug
        order
      }
    }
  }
}
